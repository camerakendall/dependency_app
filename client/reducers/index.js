// place to combine the reducers


import { combineReducers } from 'redux';
import allReducers from './allReducers';


export default combineReducers({
    // needs to create our state and initialize with reducers
        // i.e 'thisState' = allReducers,
})